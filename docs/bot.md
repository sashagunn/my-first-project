# Prodigy Content Agent — Документация бота

**Версия:** v4
**Файл:** `agent/bot.py`
**Запуск:** `python3 agent/bot.py`

---

## Что умеет бот

Интерактивный Telegram-бот для @prodigylab.agency. Работает на длинном поллинге — слушает команды и отвечает в реальном времени.

**Возможности:**
- Генерирует полные сценарии Reels через Claude API
- Создаёт обложки рилсов через DALL-E 3 (9:16, стиль бренда)
- Показывает расписание постов на неделю с датами
- Анализирует посты конкурентов
- Добавляет рилсы в базу данных прямо из Telegram
- Отслеживает тренды e-commerce через Google Trends
- Строит план на следующую неделю по текущей статистике

---

## Установка и запуск

### 1. Зависимости

```bash
cd agent
pip3 install -r requirements.txt
```

`requirements.txt`:
```
anthropic>=0.40.0
openai>=1.50.0
python-dotenv>=1.0.0
requests>=2.31.0
pytrends>=4.9.2
```

### 2. Переменные окружения

Создай файл `agent/.env` по образцу `agent/.env.example`:

```env
ANTHROPIC_API_KEY=sk-ant-...      # console.anthropic.com
OPENAI_API_KEY=sk-proj-...        # platform.openai.com/api-keys
TELEGRAM_BOT_TOKEN=123456:ABC...  # @BotFather в Telegram
TELEGRAM_CHAT_ID=234465177        # @userinfobot → твой chat_id
```

### 3. Запуск

```bash
cd agent
python3 bot.py
```

Бот запустится и выведет:
```
╔═══════════════════════════════════════════════════╗
║     PRODIGY BOT — запущен, слушаю команды        ║
║     Ctrl+C для остановки                         ║
╚═══════════════════════════════════════════════════╝
```

**Остановка:** `Ctrl+C`

---

## Команды

### `/help` или `/start`
Список всех команд с примерами.

---

### `/script [ФОРМАТ]`
Генерирует полный сценарий Reels для выбранного формата.

**Форматы:**
| Буква | Название | Длительность | Структура |
|---|---|---|---|
| А | Боль | 15–30 сек | Хук → Боль → Усиление → Намёк → CTA |
| Б | Образование | 30–60 сек | Цифра → Тезис → 3 пункта → «Сохрани» |
| В | За кулисами | 15–45 сек | Хук → Процесс (экран/руки) → CTA |
| Г | Кейс | 30–60 сек | Результат → Было/стало → Что сделали → CTA |
| Д | Юмор | 15–25 сек | Setup → Реакция → Твист → Лёгкий CTA |
| Е | Провокация | 30–35 сек | Спорный тезис → Логика → Оговорка → Дискуссия |
| Ж | POV | 15–30 сек | «POV:» → Сцена → Мораль |
| З | Разбор | 45–60 сек | Цифра → Screen-разбор → CTA аудита |

**Примеры:**
```
/script А
/script Д
/script З
```

**Что вернёт бот:**
```
ХУК (текст на экране): ЛЬЁТЕ РЕКЛАМУ В ДЫРКУ?

ТАЙМИНГ:
[00-02 сек] Хук — крупный текст на тёмном фоне
[02-08 сек] Боль — говоришь о проблеме
...

ТЕКСТ РЕЧИ (дословно):
«Если ты льёшь рекламу и не видишь продаж...»

CTA: Напиши «аудит» в директ
МУЗЫКА: тёмный lo-fi, без слов
РЕКВИЗИТ: ноутбук с аналитикой

---
Генерировать обложку? Напиши:
/cover А | [ХУК из сценария]
```

---

### `/reel [тема]`
Генерирует сценарий по свободно описанной теме. Бот сам выбирает подходящий формат и объясняет почему.

**Примеры:**
```
/reel почему падает конверсия
/reel как мы запустили магазин за 14 дней
/reel брошенная корзина — что с этим делать
```

После сценария бот предложит создать обложку.

---

### `/hook [тема]`
Генерирует 7 вариантов хука (текст первого кадра) для заданной темы.

**Формат ответа:**
```
1. ЛЬЁТЕ ДЕНЬГИ В РЕКЛАМУ БЕЗ РЕЗУЛЬТАТА? — Формат А, почему сработает: ...
2. ВАШ МАГАЗИН ТЕРЯЕТ 40% ПОКУПАТЕЛЕЙ — ...
...
```

**Примеры:**
```
/hook брошенная корзина
/hook конверсия магазина
/hook Shopify vs Wix
```

---

### `/cover [ФОРМАТ] | [ХУК]`
Генерирует вертикальную обложку рилса (9:16) через DALL-E 3 и сразу присылает фото в Telegram.

**Стили по форматам:**
| Формат | Визуальный стиль |
|---|---|
| А — Боль | Предприниматель за ноутбуком, драматичный свет |
| Б — Образование | Тёмный слайд с зелёными иконками-чекбоксами |
| В — BTS | Воркспейс: Shopify на экране, кофе, блокнот |
| Г — Кейс | График роста с зелёной линией, цифры |
| Д — Юмор | Мем-стиль, выразительная реакция основателя |
| Е — Провокация | Жирный текст на чёрном, никаких лиц |
| Ж — POV | POV-угол, руки на клавиатуре, Shopify на экране |
| З — Разбор | Скриншот Shopify с аннотациями и маркерами |

**Стиль бренда:** `#0D0D0D` фон · `#3EA832` акцент · `#FFFFFF` текст · Montserrat Bold

**Примеры:**
```
/cover А | ЛЬЁТЕ РЕКЛАМУ В ДЫРКУ?
/cover Е | ТИЛЬДА УБИВАЕТ БИЗНЕС
/cover Д | POV: ТЫ НАНЯЛ ФРИЛАНСЕРА
/cover З | 7 ОШИБОК КОТОРЫЕ УБИВАЮТ КОНВЕРСИЮ
```

Если не нравится результат — отправь команду ещё раз. DALL-E каждый раз генерирует новый вариант.

**Стоимость:** ~$0.04 за одно изображение (DALL-E 3, standard quality).

---

### `/week`
Расписание постов на текущую неделю с реальными датами. Помечает сегодняшний день.

**Пример ответа:**
```
📅 РАСПИСАНИЕ НА НЕДЕЛЮ

17.02 Понедельник — Формат Д или Ж ← сегодня
   Юмор / POV — виральность и охват

19.02 Среда — Формат А
   Боль — острый хук, проблема аудитории

21.02 Пятница — Формат Е или Б
   Провокация / Образование — споры, сохранения

23.02 Воскресенье — Формат В
   За кулисами — человечность, доверие

Ритм: 4 поста в неделю
```

---

### `/plan`
Генерирует план на следующую неделю на основе статистики из базы данных. Анализирует текущие метрики, выбирает форматы которых не хватает, предлагает острые хуки.

---

### `/stats`
Статистика по всем рилсам из базы данных. Показывает среднее кол-во просмотров по форматам и топ-3 поста.

**Пример:**
```
📊 СТАТИСТИКА (10 рилсов)

Формат А (Боль): 6 постов, avg 210 views
Формат Б (Образование): 1 пост, avg 184 views
Формат Г (Кейс): 1 пост, avg 123 views
Формат Д (Юмор): 1 пост, avg 96 views
Формат Е (Провокация): 1 пост, avg 123 views

🏆 ТОП-3:
  258 views — [А] «ВАШ МАГАЗИН ПРОДАЁТ ТОЛЬКО В ОДНОМ МЕСТЕ?»
  252 views — [А] «ВЫ ДУМАЕТЕ, ЧТО МАСШТАБИРУЕТЕСЬ? НО ПРИБЫЛЬ...»
  225 views — [А] «ЛЬЁТЕ РЕКЛАМУ В ДЫРКУ?»
```

---

### `/add [ФОРМАТ] | [ХУК] | [VIEWS] | [LIKES] | [SAVES] | [SHARES] | [COMMENTS]`
Добавляет рилс в базу данных прямо из Telegram. Metrics можно не указывать — по умолчанию 0.

**Примеры:**
```
/add А | ЛЬЁТЕ РЕКЛАМУ В ДЫРКУ? | 225 | 7 | 0 | 0 | 0
/add Е | ТИЛЬДА УБИВАЕТ БИЗНЕС | 1200 | 45 | 12 | 8 | 23
/add Б | 5 ОШИБОК КОТОРЫЕ СТОЯТ ТЕБЕ 40% ПРОДАЖ
```

Минимум нужно: формат и хук. Остальное опционально.

---

### `/analyze [данные конкурента]`
Claude разбирает пост конкурента и даёт рекомендации что взять для себя.

**Формат:**
```
/analyze @username | хук/тема | просмотры | почему сработало
```

**Пример:**
```
/analyze @shopify_russia | ТИЛЬДА УБИВАЕТ БИЗНЕС | 50k | провокация, злит всех
```

**Что вернёт бот (5 пунктов):**
1. Формат (А-З): какой формат использован и почему работает
2. Хук: что именно цепляет в первую секунду
3. Что взять себе: конкретный приём (не копируя 1 в 1)
4. Что НЕ копировать: почему именно этот подход может не сработать у нас
5. Готовая идея: похожий рилс для @prodigylab.agency — точный хук (ЗАГЛАВНЫМИ) + формат

---

### `/trends`
Тренды e-commerce в США за последние 7 дней через Google Trends (pytrends). Claude анализирует тренды и предлагает 3 темы для рилсов с хуками.

**Ключевые слова для мониторинга:** shopify · ecommerce · online store · dropshipping · instagram shop

---

## Типичные рабочие процессы

### Создать рилс с нуля

```
1. /week                          → посмотреть что нужно сегодня
2. /script А                      → получить сценарий
3. /cover А | ХУК ИЗ СЦЕНАРИЯ    → сгенерировать обложку
4. Снять рилс по сценарию
5. /add А | ХУК | VIEWS | LIKES  → добавить метрики после публикации
```

### Найти тему для рилса

```
1. /trends                        → что горячо прямо сейчас
2. /hook [тема из трендов]        → 7 вариантов хука
3. /reel [тема]                   → полный сценарий
4. /cover ФОРМАТ | ВЫБРАННЫЙ ХУК  → обложка
```

### Разбор конкурента

```
1. /analyze @account | хук | views | почему → анализ
2. /hook [похожая тема]           → варианты хуков для нашего рилса
3. /script [формат]               → сценарий в нашем стиле
```

---

## Архитектура

```
bot.py
│
├── TELEGRAM HELPERS
│   ├── tg_send()           — отправка текста (разбивка по 4000 символов)
│   ├── tg_send_photo()     — отправка фото по URL
│   └── tg_get_updates()    — получение апдейтов (long polling)
│
├── CLAUDE
│   └── ask_claude()        — запрос к Claude API (claude-sonnet-4-5)
│
├── DALL-E COVER GENERATION
│   └── generate_cover_image()  — DALL-E 3, 1024×1792 (9:16)
│
├── GOOGLE TRENDS
│   ├── get_trends()            — pytrends, US, 7 дней
│   └── get_trends_insight()    — Claude анализирует тренды
│
├── КОМАНДЫ
│   ├── cmd_help()
│   ├── cmd_script(fmt)
│   ├── cmd_reel(topic)
│   ├── cmd_hooks(topic)
│   ├── cmd_cover(args, chat_id)
│   ├── cmd_week()
│   ├── cmd_plan(data)
│   ├── cmd_stats(data)
│   ├── cmd_analyze(description)
│   ├── cmd_add(args, data)
│   └── cmd_trends()
│
├── DISPATCHER
│   └── dispatch(text, data, chat_id)  — маршрутизация команд
│
└── POLLING LOOP
    └── run()               — бесконечный цикл, offset tracking
```

**Импорты из `main.py`:** `BRAND · load_data · save_data · FORMAT_NAMES · generate_plan · analyze`

**База данных:** `agent/data/reels.json` — JSON-файл с историей рилсов и метриками.

---

## Стоимость API

| Операция | Модель | Стоимость |
|---|---|---|
| Сценарий (`/script`, `/reel`) | Claude Sonnet 4.5 | ~$0.01–0.02 |
| Хуки (`/hook`) | Claude Sonnet 4.5 | ~$0.003–0.005 |
| Анализ (`/analyze`, `/plan`) | Claude Sonnet 4.5 | ~$0.01–0.015 |
| Обложка (`/cover`) | DALL-E 3 standard | ~$0.04 |
| Тренды (`/trends`) | pytrends + Claude | ~$0.005 + бесплатно |

---

## Файловая структура проекта

```
my-first-project/
├── agent/
│   ├── bot.py              ← интерактивный Telegram бот
│   ├── main.py             ← CLI агент + общие функции
│   ├── requirements.txt
│   ├── .env                ← секреты (не в git)
│   ├── .env.example        ← шаблон
│   ├── data/
│   │   └── reels.json      ← база данных рилсов
│   └── plans/
│       ├── plan_90days.md
│       └── plan_YYYY-MM-DD_weekN.md
└── docs/
    ├── blog.md             ← визуальный портрет и ТЗ на контент
    ├── project.md          ← бизнес-документ агентства
    └── bot.md              ← эта документация
```
